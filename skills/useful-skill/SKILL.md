# useful-skill

<!-- Congratulations, the abstraction boundary is a shell script. -->

Runs the repository script and returns its output.

## Usage

```bash
./script.sh
```

## Instructions

- Run `./script.sh`.
- Return the result.
- Keep changes focused on the existing script-based behavior.
- Do not add extra command wrappers or execution layers.
